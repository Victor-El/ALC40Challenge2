<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.victor.alc40challenge2">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".DisplayTravels"
            android:label="@string/title_activity_display_travels"
            android:theme="@style/AppTheme.NoActionBar"></activity>
        <activity
            android:name=".AddTravel"
            android:label="@string/title_activity_add_travel"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity android:name=".GoogleSignIn" />
        <activity android:name=".SignIn" />
        <activity android:name=".SignUp" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>